import styled from 'styled-components';

export const Button = styled.button`
  background: linear-gradient(45deg, var(--blush), var(--coral));
  border: none;
  height: 2.5rem;
  border-radius: var(--border-radius);
  padding: 0.5rem 1rem;
`
